import { RegistroNota } from "./registronota";

export enum Situação{ //BOLETIM
    CURSANDO = 'Cursando',
    APROVADO = 'Aprovado(a)',
    PROVAFINAL = 'Prova final',
    REPROVADO = 'Reprovado(a)'
}
